package com.arhohuttunen.junit5.kotlin

class Calculator {

    fun add(first: Int, second: Int) = first + second

    fun divide(numerator: Int, denominator: Int) = numerator / denominator

    fun square(number: Int) = number * number
}